<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

    <entry key="trafficIncidents.findByDateRange">
        {lastModified: {$gte: #, $lte: #}}
    </entry>

    <entry key="favoriteRoute.findByUserId">
        {userId: #}
    </entry>

    <entry key="favoriteRoute.findByNotificationTimeRange">
        {notificationTimes: {$gte: #, $lte: #}}
    </entry>

</properties>